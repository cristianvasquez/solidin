import { Representation } from '../../ldp/representation/Representation';
import { RepresentationConverter, RepresentationConverterArgs } from './RepresentationConverter';
/**
 * Converts `text/turtle` to `internal/quads`.
 */
export declare class TurtleToQuadConverter extends RepresentationConverter {
    canHandle(input: RepresentationConverterArgs): Promise<void>;
    handle(input: RepresentationConverterArgs): Promise<Representation>;
    private turtleToQuads;
}
