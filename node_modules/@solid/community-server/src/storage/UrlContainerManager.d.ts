import { RuntimeConfig } from '../init/RuntimeConfig';
import { ResourceIdentifier } from '../ldp/representation/ResourceIdentifier';
import { ContainerManager } from './ContainerManager';
/**
 * Determines containers based on URL decomposition.
 */
export declare class UrlContainerManager implements ContainerManager {
    private readonly runtimeConfig;
    constructor(runtimeConfig: RuntimeConfig);
    getContainer(id: ResourceIdentifier): Promise<ResourceIdentifier>;
    private canonicalUrl;
}
