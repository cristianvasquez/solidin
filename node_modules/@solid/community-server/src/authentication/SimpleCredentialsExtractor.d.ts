import { HttpRequest } from '../server/HttpRequest';
import { Credentials } from './Credentials';
import { CredentialsExtractor } from './CredentialsExtractor';
/**
 * Credentials extractor which simply interprets the contents of the Authorization header as a webID.
 */
export declare class SimpleCredentialsExtractor extends CredentialsExtractor {
    canHandle(): Promise<void>;
    handle(input: HttpRequest): Promise<Credentials>;
}
