/**
 * This class holds all configuration options that can be defined by the user via the command line.
 *
 * Concretely, this contains data that is only relevant *after* dependency injection.
 */
export declare class RuntimeConfig implements RuntimeConfigData {
    private pport;
    private pbase;
    private prootFilepath;
    constructor(data?: RuntimeConfigData);
    reset(data: RuntimeConfigData): void;
    /**
     * The returned URL is ensured to have a trailing slash.
     */
    get base(): string;
    get port(): number;
    get rootFilepath(): string;
}
export interface RuntimeConfigData {
    port?: number;
    base?: string;
    rootFilepath?: string;
}
