import { Operation } from '../operations/Operation';
import { PermissionSet } from './PermissionSet';
import { PermissionsExtractor } from './PermissionsExtractor';
/**
 * Generates permissions for a SPARQL DELETE/INSERT patch.
 * Updates with only an INSERT can be done with just append permissions,
 * while DELETEs require write permissions as well.
 */
export declare class SparqlPatchPermissionsExtractor extends PermissionsExtractor {
    canHandle(input: Operation): Promise<void>;
    handle(input: Operation): Promise<PermissionSet>;
    private isSparql;
    private isDeleteInsert;
    private needsAppend;
    private needsWrite;
}
