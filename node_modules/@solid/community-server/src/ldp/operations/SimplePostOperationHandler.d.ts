import { ResourceStore } from '../../storage/ResourceStore';
import { Operation } from './Operation';
import { OperationHandler } from './OperationHandler';
import { ResponseDescription } from './ResponseDescription';
/**
 * Handles POST {@link Operation}s.
 * Calls the addResource function from a {@link ResourceStore}.
 */
export declare class SimplePostOperationHandler extends OperationHandler {
    private readonly store;
    constructor(store: ResourceStore);
    canHandle(input: Operation): Promise<void>;
    handle(input: Operation): Promise<ResponseDescription>;
}
