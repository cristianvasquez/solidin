{
  "name": "solidin",
  "version": "0.0.1",
  "description": "quick look into the server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@solid/community-server": "^0.1.1",
    "cors": "^2.8.5"
  },
}
